[project]
name = "tonyscrapper"
version = "1.0.0"
description = "AI-powered web scraping and PDF processing toolkit"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}

[project.scripts]
tonyscrapper = "app_launcher:main"
