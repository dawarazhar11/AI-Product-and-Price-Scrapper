[project]
name = "ai-product-and-price-scrapper"
version = "1.0.0"
description = "AI-powered web scraping and PDF processing toolkit"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}

[project.scripts]
ai-scrapper = "app_launcher:main"
